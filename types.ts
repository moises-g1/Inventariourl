// // types.ts

// import firebase from 'firebase/compat/app';
// import 'firebase/compat/database';

// export interface IteratedDataSnapshot extends firebase.database.DataSnapshot {
//   key: string | null; // Permitir que la propiedad key sea nullable
//   val(): any;
//   exists(): boolean;
//   forEach(action: (a: IteratedDataSnapshot) => boolean | void): boolean;
// }
